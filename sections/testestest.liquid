{% liquid
  assign product = all_products['urtopia-chord-ebike']
%}

<section class="er-max-w-screen-xl er-mx-auto er-grid er-grid-cols-3">
  <div class="er-bg-dark"></div>
  <div></div>
  <div class="er-bg-white">
    <h1>{{ product.title }}</h1>
  </div>
</section>
