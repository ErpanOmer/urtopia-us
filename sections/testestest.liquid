{% liquid
  assign product = all_products['urtopia-chord-ebike']
  assign a = product.variants | where: "id", 43694976663800 | first
%}

<script>
  console.log(JSON.parse(JSON.stringify({{ product | json }})))
  // console.log(JSON.parse(JSON.stringify({{  }})))
</script>


<section class="er-max-w-screen-xl er-mx-auto er-flex er-justify-between er-px-12 er-m-48">
  <div class="er-bg-dark er-w-24 er-shrink-0">1</div>
  <div class="er-flex-1 er-px-[5%]">
    <div class="er-aspect-video">
      {% for variant_image in a.metafields.custom.variant_image.value %}
         variant_image
        {{ variant_image | image_url: width: 400 }}
      {% endfor %}
    </div>
  </div>
  <div class="er-bg-white er-min-w-[400px] er-shrink-0">
    <h1>{{ product.title }}</h1>
  </div>
</section>
